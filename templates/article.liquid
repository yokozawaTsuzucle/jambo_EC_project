<div class="subpage_bg_img d-flex justify-content-center align-items-center"
  style="background-image: url('{% if article.image %}{{article.image.src | img_url: '1920x'}}{% else %}何らかのURL{% endif %}');">
</div>
{% render 'breadcrumbs' %}
<div class="main">
  <div class="container">
    <div class="row article">
      <div class="col-9">
        <h1 class="f36 mb-3">{{article.title}}</h1>
        <div class="d-flex mb-1">
          <small class="text-muted mr-2">{{ article.created_at | date: '%Y/%m/%d'}}</small>
          
        </div>
        <div class="d-flex mb-1 articles-category">
          {% for tag in article.tags %}
          <small class="text-muted mr-2">{{tag}}</small>
          {% endfor %}
        </div>
        <div class="mt-5 mb-5">
          <p>{{ article.content }}</p>
        </div>
        <p><a href="{{blog.url}}"><i class="fas fa-arrow-left mr-1"></i>一覧へ戻る</a></p>
      </div>
      <div class="col-3">
        <h5>カテゴリー</h5>
        <ul class="articles-category">
          {% for tag in blog.all_tags %}
          <li class="mb-1"><a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag }}</a></li>
          {% endfor %}
        </ul>
      </div>
    </div>

  </div>
</div>